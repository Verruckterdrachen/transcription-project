```markdown
# 📋 AI Checklist - Что проверить перед отправкой решения

**Версия:** v16.24  
**Дата:** 2026-02-13  
**Цель:** Гарантировать, что AI никогда не забудет напомнить про тесты

---

## ✅ Перед отправкой кода

### Для БАГОВ (ОБЯЗАТЕЛЬНО):

- [ ] Прочитал **DEBUGGING.md** — понял RCA
- [ ] Добавил **DEBUG OUTPUT** перед фиксом
- [ ] Нашёл **ROOT CAUSE** (5 Whys)
- [ ] Создал **unit test** (failing test в `tests/`)
- [ ] Исправил баг
- [ ] **ЯВНО СКАЗАЛ:** "Запусти тесты ПЕРЕД коммитом"
- [ ] Показал команду: `python -m pytest tests/test_*.py -v`
- [ ] Предупредил: "Коммитим ТОЛЬКО после GREEN"
- [ ] Обновил **VERSION.md**
- [ ] Показал **git команды** с пометкой "(ТОЛЬКО ПОСЛЕ GREEN)"

### Для ФИЧ:

- [ ] Код готов
- [ ] Если есть тесты → напомнил запустить
- [ ] Обновил **VERSION.md** (если значимо)
- [ ] Показал git команды

### Для ВСЕХ изменений:

- [ ] Присылаю **ПОЛНЫЙ файл** (не фрагменты)
- [ ] Показал git команды
- [ ] Объяснил **ROOT CAUSE** (для багов)
- [ ] Обновил **transcribe.py** (VERSION + VERSION_NAME)

---

## ❌ Что ЗАПРЕЩЕНО

### Критические ошибки:

- ❌ Говорить "коммить" БЕЗ напоминания про тесты (для багов)
- ❌ Отправлять код БЕЗ unit tests (для багов)
- ❌ Пропускать VERSION.md
- ❌ Коммитить на RED тестах
- ❌ Предполагать, что пользователь сам вспомнит про тесты

### Методологические ошибки:

- ❌ Исправлять БЕЗ понимания ROOT CAUSE
- ❌ Менять несколько вещей одновременно
- ❌ Гадать гипотезы без анализа
- ❌ Пропускать DEBUG OUTPUT

---

## 📝 Шаблон отправки решения

### Для БАГА:

✅ РЕШЕНИЕ v16.X

━━━ 1. scripts/path/file.py ━━━
[полный код с фиксом]

━━━ 2. tests/test_name.py ━━━
[unit test для бага]

━━━ 3. docs/VERSION.md ━━━
[обновлённая версия]

━━━ ПЕРЕД КОММИТОМ: Запусти тесты! ━━━

python -m pytest tests/test_name.py -v

✅ Если GREEN → коммить
❌ Если RED → присылай ошибку, фиксим

━━━ Git команды (ТОЛЬКО ПОСЛЕ GREEN) ━━━

git add scripts/path/file.py tests/test_name.py docs/VERSION.md
git commit -m "🔧 vX.X: описание бага + ROOT CAUSE"

text

### Для ФИЧИ:

✅ РЕШЕНИЕ v16.X

━━━ 1. scripts/path/file.py ━━━
[полный код]

━━━ 2. docs/VERSION.md ━━━
[обновлённая версия]

━━━ Git команды ━━━

git add scripts/path/file.py docs/VERSION.md
git commit -m "🆕 vX.X: описание фичи"

text

---

## 🔍 Самопроверка перед отправкой

Задай себе эти вопросы:

1. **Для бага:** Я напомнил про тесты? → ДА/НЕТ
2. **Для бага:** Я показал команду `pytest`? → ДА/НЕТ
3. **Для бага:** Я предупредил "ТОЛЬКО ПОСЛЕ GREEN"? → ДА/НЕТ
4. Я обновил VERSION.md? → ДА/НЕТ
5. Я показал git команды? → ДА/НЕТ
6. Я объяснил ROOT CAUSE (для бага)? → ДА/НЕТ

**Если хоть один НЕТ → вернись и исправь!**

---

## 📊 Статистика качества

**Цель:** Bug-to-fix ≤3 итерации (было 8)

**Метрики:**
- Баги с unit tests: **100%** (было 0%)
- Напоминания про тесты: **100%** (было 0%)
- Коммиты на GREEN: **100%** (было ~70%)

**Версия чеклиста:** v16.24  
**Последнее обновление:** 2026-02-13