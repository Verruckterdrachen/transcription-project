# 📜 История изменений (Changelog)

Полная история всех версий. Текущая версия — в **VERSION.md**.

---
[v17.1] - 2026-02-18
...

text

**Действие:** Открываешь файл → вставляешь новый блок **после заголовка `# 📜 История изменений`** → сохраняешь.

⚠️ **ВАЖНО:** CHANGELOG.md НИКОГДА не заменяется целиком — только prepend нового блока!

#### Файлы, в которые AI присылает ТОЛЬКО ИЗМЕНЁННЫЕ СТРОКИ:

- ✅ `docs/ARCHITECTURE.md`
- ✅ `docs/DEBUGGING.md`
- ✅ `docs/VALIDATION.md`

**Формат:**
Найди строку:
| 8 | split_mixed_speaker_segments | ...

Замени на:
| 8 | split_mixed_speaker_segments | ... [обновлённое описание]

text

**Действие:** Ctrl+F → находишь старую строку → заменяешь → сохраняешь.

---

**CHECKLIST перед коммитом:**
- [ ] Все `scripts/*.py` заменены целиком
- [ ] `VERSION.md` заменён целиком
- [ ] `KNOWN-ISSUES.md` заменён целиком
- [ ] `CHANGELOG.md` — новый блок вставлен в начало (НЕ заменён целиком!)
- [ ] Патчи в больших docs/ применены (если были)

## [v17.0] - 2026-02-17 — BASELINE

**Откат к v16.28** (последняя стабильная до регрессий v16.29–v16.43)

### Изменения
- ✅ Создан Golden Dataset (NW_Uckpa0001_01)
- ✅ Обновлён WORKFLOW.md (строгий порядок: Tests → Debug → Validation → Docs → Commit)
- ✅ Обновлён VALIDATION.md (8 пунктов + regression)
- ✅ Новый формат коммитов: `🔧 vX.X: FIX БАГ #N [AUDIO_FILE]`
- ⚠️ Открытые баги: #15 (пропуск текста), #17 (inner timestamps)

### Уроки v16.29–v16.43
Фиксили без regression-тестов на baseline audio → накопили регрессии за 14 итераций.
**Решение:** Golden Dataset + обязательный [AUDIO_FILE] в коммите.

---

## [v16.43] - 2026-02-17

**FIX 3 бага:** Точки без пробелов + Timestamp интервал + False positive «вопрос»

### ROOT CAUSES
- БАГ #12: `''.join(text_parts)` без пробела → `text_parts.append(sent + ' ')`
- БАГ #14: `last_timestamp_time` сбрасывался для каждого сегмента → `global_last_timestamp_time`
- БАГ #13: Pattern `r'\bвопрос'` без контекста → context check `r'\bвопрос[,\s]+(что|как)'`

---

## [v16.40] - 2026-02-17

**FIX БАГ #11:** Изменён порядок этапов: SPLIT → TIMESTAMP INJECTION

### ROOT CAUSE
Timestamp injection ДО split → вставка между словами (8/19 без точки).
Continuation patterns `r'^несмотря\b'` не срабатывали из-за timestamp в начале строки.

### Решение
`MERGE → SPLIT → TIMESTAMP INJECT` (порядок изменён)
Убран workaround в `is_continuation_phrase()`.

---

## [v16.39] - 2026-02-17

**FIX БАГ #10:** Continuation phrases с timestamps («несмотря» → Журналист)

### ROOT CAUSE
`r'^несмотря\b'` не работал при `«00:16:06 Несмотря...»` — timestamp перед словом.
Решение: `re.sub(r'^\s*\d{2}:\d{2}:\d{2}\s+', '', text)` перед проверкой.
⚠️ Workaround, не ROOT CAUSE — исправлен в v16.40.

---

## [v16.30] - 2026-02-16

**FIX БАГ #4:** Gap duplication filter (N-gram overlap + удаление пунктуации)

### ROOT CAUSE
Пунктуация в триграммах: `«в лице тигров,»` ≠ `«в лице тигров.»` → дубль не найден.
Решение: `re.sub(r'[^\w\s]', '')` перед split() + N-gram overlap для prev_segment.

---

## [v16.29] - 2026-02-16

**FIX БАГ #3:** Gap hallucination filter (threshold 55%)

### ROOT CAUSE
Whisper распознавал шумы как текст, схожий с соседним сегментом.
Решение: similarity >55% с next_text → SKIP gap.

---

## [v16.28] - 2026-02-16 ← БАЗА ДЛЯ v17.0

**FIX БАГ #2:** Потеря последнего предложения при timestamp injection

### ROOT CAUSE
`range(0, len(sentences)-1, 2)` → пропуск последнего элемента при нечётной длине.
Решение: `range(0, len(sentences), 2)`.

---

## [v16.27] - 2026-02-15

**Новая стратегия:** Real tests > Unit tests

### Урок v16.22–v16.26
8 итераций: unit tests GREEN → коммит → real test → ❌ новый баг.
Решение: Удалены 7 unit test файлов. Обязательный real test ПЕРЕД коммитом.

OLD: Баг → Unit test → pytest GREEN → Коммит → Real test → ❌
NEW: Баг → DEBUG → ROOT CAUSE → FIX → Real test → ✅ Коммит

text

---

## [v16.26] - 2026-02-13

**FIX:** Дубли timestamp — синхронизация `replica_merger` + `txt_export`

### ROOT CAUSE
Два модуля добавляли inner timestamps независимо.
Решение: `txt_export` проверяет `re.search(r'\d{2}:\d{2}:\d{2}', text)` перед вставкой.

---

## [v16.5] - 2026-02-10

Smart GAP Attribution — `text_similarity()` для атрибуции пропущенных сегментов.

---

## [v16.4] - 2026-02-08

Speaker Attribution Protection — context window, confirmation patterns.

---

## [v16.3] - 2026-02-05

Performance Boost — ускорение диаризации на 20%, снижение VRAM на 15%.

---

## [v16.2] - 2026-02-03

Text Corrections — whitelist военных терминов, коррекция ошибок Whisper.

---

## [v16.1] - 2026-02-01

Bug Fixes — пустые сегменты после merge, deduplication для коротких фраз.

---

## [v16.0] - 2026-01-28

Base Release — модульная архитектура (`core/`, `corrections/`, `merge/`, `export/`).

---

## [v15.0 и ранее] - до 2026-01-28

- v15.0: Weighted Classification (весовая система атрибуции спикеров)
- v14.0: GAP Detection (force_transcribe для пропущенных сегментов)
- v13.0: Adaptive VAD
- v12.0: Deduplication
- v11.0: Boundary Correction
- v10.0: Whisper large-v3-turbo
- v9.0: Pyannote Audio 3.1
- v1.0–v8.0: Базовый прототип → JSON/TXT экспорт → Speaker roles

---

**Последнее обновление:** 2026-02-18
**Актуальная версия:** → см. VERSION.md