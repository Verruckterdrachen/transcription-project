## 🆕 v17.0: ОБНОВЛЁННЫЙ WORKFLOW

### ПРАВИЛО #1: BASELINE AUDIO
**Всё тестируется на:** NW_Uckpa0001_01 (Исаев 27.05)

Этот файл - наш **baseline**. Перед каждым коммитом:
- Real test на baseline audio
- Regression test (8 пунктов VALIDATION.md)
- Сравнение с golden-dataset/v17.0-baseline/

### ПРАВИЛО #2: АУДИО В КОММИТЕ
🔧 v17.X: FIX БАГ #N [NW_Uckpa0001_01] - описание
^^^^^^^^^^^^^^^^
ОБЯЗАТЕЛЬНО!

text

### ПРАВИЛО #3: DOCS ОБНОВЛЯЮТСЯ ПОСЛЕДНИМИ
Баг → Debug → Fix → Real Test → Regression Test → ✅ GREEN → Docs → Commit
^^^^^^^^
ТОЛЬКО ПОСЛЕ GREEN!

text

### ПРАВИЛО #4: МАКСИМУМ DEBUG
- Добавляй debug output ПЕРЕД фиксом
- 5 Whys → ROOT CAUSE
- Документируй в VERSION.md

### ПРАВИЛО #5: NO COMMIT БЕЗ GREEN
- 0/8 checks failed = GREEN ✅ → можно коммитить
- 1+ check failed = RED ❌ → НЕ коммитить!


Автоматическая транскрибация интервью с разделением спикеров.  
Цель этого документа — зафиксировать **единый, простой и надёжный** процесс работы без unit‑тестов, опираясь на реальные аудио, логи и golden‑dataset.

---

## 1. Принципы

- **GitHub — источник правды.** Всегда работаем с актуальным `main` и текущей версией из `docs/VERSION.md`.
- **Pipeline‑first.** Любое изменение проверяется через полный прогон пайплайна на реальном аудио.
- **Validation‑first.** Качество оцениваем по `docs/VALIDATION.md`, а не по unit‑тестам.
- **Golden‑dataset важнее синтетики.** Регрессию отслеживаем по реальным интервью, а не по искусственным примерам.
- **Root Cause Analysis (5 Whys).** Каждый баг сначала понимаем, потом чиним.

Unit‑тесты в этом проекте **не используются**. Вместо этого у нас есть:
- реальный пайплайн `scripts/transcribe.py`,
- реальный звук (особенно интервью Исаева),
- `test-results/latest/` и golden‑dataset,
- чек-листы VALIDATION и DEBUGGING.

---

## 2. Роли: человек и AI

### Ты (человек)

- Держишь локальную копию репозитория, работаешь в ветке `main`.
- Запускаешь пайплайн на аудио, смотришь JSON/TXT/LOG.
- Проверяешь результат по `docs/VALIDATION.md`.
- Коммитишь изменения, когда всё зелёное.

### Я (AI)

- Читаю текущую архитектуру (по коду и `docs/`).
- Предлагаю фикс или доработку в виде **готовых файлов целиком**.
- Обновляю `docs/VERSION.md` (описание версии, ROOT CAUSE, что изменилось в коде).
- Не использую unit‑тесты, не прошу запускать `pytest`.

---

## 3. Основной цикл работы

### Шаг 0. Подготовка

1. Обнови локальный репозиторий:
   ```bash
   git pull origin main
Открой docs/VERSION.md — это текущая версия пайплайна.

Проверь структуру папок для нового спикера:

text
Спикер (ДД.ММ)/
  audio/   # WAV
  json/    # будет создано
  txt/     # будет создано
Шаг 1. Запуск пайплайна
Запускаем scripts/transcribe.py:

bash
cd scripts
python transcribe.py
В интерактивном режиме:

Вводишь путь к папке спикера (Спикер (ДД.ММ)/).

Выбираешь режим (обычно [точный] — по умолчанию).

Ждёшь окончания работы:

JSON в .../json/,

TXT в .../txt/,

DEBUG‑лог в transcription_debug.log,

копия результата в test-results/latest/.

Шаг 2. Валидация результата
По окончании см. docs/VALIDATION.md и проверь 7 пунктов:

нет длинных кусков >30 сек без внутренних timestamp,

нет дублей фраз,

заикания помечены ...,

нет «Продолжение следует» и похожих хвостов,

нет двух реплик одного спикера подряд,

timestamp‑ы не идут назад,

все большие гапы >3 сек либо объяснены, либо force‑transcribe.

Если хоть один пункт не выполняется — это баг → см. раздел 4.

Шаг 3. Фикс и обновление версии
Если нужно изменить код:

Формулируем задачу и отправляем мне (AI).

Я присылаю:

полный текст изменённых scripts/*.py,

обновлённый docs/VERSION.md,

при необходимости — обновления других .md.

Ты заменяешь файлы целиком и запускаешь пайплайн снова на реальном аудио (минимум 1 интервью).

Когда результат устраивает и чек‑лист VALIDATION зелёный:

Обновлённый docs/VERSION.md уже описывает:

версию (например, v16.26),

дату,

ROOT CAUSE,

список изменений,

выводы/уроки.

Выполняешь:

bash
git add scripts/ docs/
git commit -m "🔧 v16.XX: краткое описание фикса + ROOT CAUSE"
git push
4. При обнаружении бага
Если на любом этапе находишь аномалию — это не “особенность”, а баг.

4.1. Минимальный сценарий воспроизведения
Зафиксируй, на каком аудио и в каком месте проявляется ошибка:

timestamp,

текст вокруг,

спикер.

Убедись, что входное аудио и настройки пайплайна стандартные.

4.2. Root Cause Analysis (5 Whys)
Следуем docs/DEBUGGING.md:

добавляем временные print()/логирование в нужных местах пайплайна,

выясняем, на каком этапе (диаризация, merge, экспорт и т.д.) и какая функция ломает данные,

отвечаем себе на вопрос «почему?» 5 раз, пока не найдём реальный источник.

4.3. Фикс и проверка
Вносим точечный фикс в код (одну причину за раз).

Запускаем полный пайплайн:

сначала на проблемном файле,

затем на 1–2 файлах из golden‑dataset.

Если валидация пройдена:

описываем баг и фикс в docs/VERSION.md,

коммитим в main.

5. Golden‑dataset
Golden‑dataset — это набор реальных интервью, на которых пайплайн обязан давать стабильный, проверенный результат.

Использование:

каждый серьёзный фикс → прогон минимум на одном файле из golden‑dataset;

перед крупными изменениями (архитектура, логика merge, экспорт) — прогон на всех файлах golden‑dataset;

если результат изменился, но стал лучше — обновляем golden‑dataset и документируем это в VERSION.md.

Golden‑dataset важнее любых синтетических тестов: именно он гарантирует, что пайплайн не «починили» в одном месте ценой слома в другом.

6. Что мы НЕ делаем
Не пишем unit‑тесты. Они уже показали слабую применимость к нашему сценарию (не ловят ошибки взаимодействия модулей и реальных текстов).

Не оптимизируем пайплайн под синтетические случаи. Только реальные аудио.

Не коммитим изменения без обновлённого docs/VERSION.md.

Главный критерий успеха:
«Реальное интервью прогналось без критичных ошибок, текст и структура соответствуют ожиданиям, чек‑лист VALIDATION зелёный».
======================================================================

## 🧪 Правило тестирования (v16.27)

### Для БАГОВ (ОБЯЗАТЕЛЬНО):
✅ AI добавляет **DEBUG OUTPUT** в проблемный модуль  
✅ AI находит **ROOT CAUSE** (5 Whys)  
✅ AI исправляет баг  
✅ Разработчик запускает **REAL TEST** на реальном аудио  
✅ Проверка **VALIDATION.md checklist** (7 пунктов)  
✅ Коммит **ТОЛЬКО после** ✅ checklist GREEN  

### Для ФИЧ:
⚠️ Если есть integration test → запустить  
⚠️ Real test рекомендуется  

---

### 🆕 v16.27: Урок от v16.22-v16.26

**ПРОБЛЕМА:**
- v16.22-v16.24: Unit tests → pytest GREEN → коммит → real test → ❌ БАГ!
- 8 итераций вместо 1-2

**РЕШЕНИЕ:**
- Отказ от unit tests для багов взаимодействия модулей
- ОБЯЗАТЕЛЬНЫЙ real test ПЕРЕД коммитом

**ФИЛОСОФИЯ v16.27+:**
```
OLD: Баг → Unit test → pytest → Коммит → Real test → ❌ БАГ!
NEW: Баг → DEBUG → ROOT CAUSE → FIX → Real test → ✅ Коммит!
```
