```markdown
# 🔄 Workflow взаимодействия

Подробное описание процесса работы AI Assistant с проектом через GitHub.

---

## 📋 Формат работы

### Основной принцип: **КОД В ЧАТЕ → ТЕСТИРОВАНИЕ → КОММИТ В MAIN**

**AI (я):**
1. Читаю актуальный код из GitHub через MCP tools
2. Анализирую задачу (RCA для багов)
3. Создаю unit tests (для багов)
4. Присылаю готовые файлы в чат (полный код)
5. **🆕 ЯВНО НАПОМИНАЮ: "Запусти тесты ПЕРЕД коммитом"**
6. Проверяю изменения после коммита
7. Подтверждаю синхронизацию

**Разработчик (ты):**
1. Копируешь код из чата в локальные файлы
2. **🆕 ЗАПУСКАЕШЬ ТЕСТЫ** (для багов/фич с тестами)
   ```bash
   python -m pytest tests/test_*.py -v
Если GREEN → Коммитишь в main

Если RED → Присылаешь ошибку мне

Уведомляешь: "Готово" (после GREEN + коммита)

🧪 Правило тестирования (v16.24)
Для БАГОВ (ОБЯЗАТЕЛЬНО):
✅ AI создаёт unit test (failing test)

✅ AI исправляет баг

✅ AI ЯВНО НАПОМИНАЕТ: "Запусти тесты ПЕРЕД коммитом"

✅ Разработчик запускает pytest ПЕРЕД коммитом

✅ Коммит ТОЛЬКО после GREEN

✅ Если RED → присылаешь ошибку → AI фиксит → повтор

Для ФИЧ (опционально):
⚠️ Если есть тесты → AI напоминает запустить

⚠️ Если нет тестов → можно коммитить без тестирования

Формат напоминания от AI:
text
━━━ ПЕРЕД КОММИТОМ: Запусти тесты! ━━━

python -m pytest tests/test_txt_export.py -v
python -m pytest tests/test_boundary_fixer.py -v

✅ Если GREEN → коммить
❌ Если RED → присылай ошибку, фиксим

━━━ Git команды (ТОЛЬКО ПОСЛЕ GREEN) ━━━

## 🔢 VERSION MANAGEMENT (КРИТИЧЕСКИ ВАЖНО!)

### ⚠️ ПРАВИЛО: При КАЖДОМ изменении кода обновляю ТРИ места:

```python
# 1. scripts/transcribe.py (строки ~70-71)
VERSION = "16.9"  # ← ОБЯЗАТЕЛЬНО обновляю!
VERSION_NAME = "Continuation Phrase Fix"  # ← ОБЯЗАТЕЛЬНО обновляю!

# 2. docs/VERSION.md
# Version: v16.9
**Кодовое имя:** Continuation Phrase Fix

# 3. Измененные модули (комментарии в начале файла)
# Version: v16.9
# Last updated: 2026-02-11
Почему это критично:
VERSION в transcribe.py → это то, что показывается в логах транскрипции

Без обновления VERSION → Python не перезагружает модули (кэширование)

Результат: используется старый код, даже если модули обновлены!

✅ Правильный порядок:
bash
# Шаг 1: AI присылает в чат
1. scripts/transcribe.py (с обновлённым VERSION)
2. scripts/corrections/speaker_classifier.py (с изменениями)
3. docs/VERSION.md (с новой версией)

# Шаг 2: Разработчик копирует и коммитит
git add scripts/transcribe.py scripts/corrections/speaker_classifier.py docs/VERSION.md
git commit -m "🔧 vX.X: описание изменения"
git push origin main

# Шаг 3: Тестирование
python scripts/transcribe.py
# В логах должна быть НОВАЯ версия!
❌ Что НЕЛЬЗЯ делать:
bash
# НЕ коммитить модули без обновления transcribe.py
git add scripts/corrections/speaker_classifier.py  # ← только модуль
git commit -m "fix"
# Результат: VERSION в логах старая, код кэшируется!

# НЕ обновлять только VERSION.md
# Результат: документация не соответствует коду!
🔍 Проверка после теста:
python
# В начале лога ОБЯЗАТЕЛЬНО должна быть НОВАЯ версия:
🔥 ПАЙПЛАЙН v16.9: Continuation Phrase Fix  # ← Правильно!

# НЕ должно быть:
🔥 ПАЙПЛАЙН v16.8: ...  # ← Старая версия = БАГ!
🔍 ЭТАП 1: Аудит проекта (первый запрос)
Когда делать аудит:
✅ При первом запросе в новой сессии

✅ После долгого перерыва (>1 день)

✅ Когда версия неизвестна

✅ Перед масштабными изменениями

Процесс аудита:
python
# 1. Читаю VERSION.md
mcp_tool_github-mcp-direct_get_file_contents(
    owner="Verruckterdrachen",
    repo="transcription-project",
    path="docs/VERSION.md"
)
# → Запоминаю текущую версию (например: v16.5)

# 2. Проверяю структуру scripts/
mcp_tool_github-mcp-direct_get_file_contents(
    owner="Verruckterdrachen",
    repo="transcription-project",
    path="scripts"
)

# 3. Читаю главный скрипт
mcp_tool_github-mcp-direct_get_file_contents(
    owner="Verruckterdrachen",
    repo="transcription-project",
    path="scripts/transcribe.py"
)
# → Проверяю VERSION и VERSION_NAME (строки ~70-71)

# 4. Проверяю все модули
for module in ["core", "corrections", "merge", "export"]:
    get_file_contents(f"scripts/{module}")

# 5. Проверяю тестовые данные
get_file_contents("test-results/latest")

# 6. Смотрю последние коммиты
list_commits(sha="main", perPage=10)
[... остальная часть WORKFLOW.md без изменений ...]

📦 Какие файлы я присылаю
ОБЯЗАТЕЛЬНО (при каждом изменении):
✅ scripts/transcribe.py (с обновлённым VERSION и VERSION_NAME)
✅ Изменённый код (полный файл, готовый для копирования)
✅ docs/VERSION.md (ВСЕГДА обновляю версию!)

ОПЦИОНАЛЬНО (по необходимости):
⚠️ docs/MODULES.md - если:

Добавлена новая функция

Изменилась сигнатура функции

Изменилось поведение (новые параметры, return types)

⚠️ docs/ARCHITECTURE.md - если:

Добавлен новый этап в pipeline

Изменён порядок обработки

Добавлен новый модуль в структуру

⚠️ docs/CHANGELOG.md - если:

Major update (v16.x → v17.0)

Breaking changes

Значительный bug fix

⏱️ Примерное время цикла

| Этап             | Время   | Описание                    |
| ---------------- | ------- | --------------------------- |
| Аудит проекта    | 30-60s  | При первом запросе          |
| Чтение кода      | 10-15s  | 1-3 файла                   |
| Анализ + решение | 30-60s  | Создание кода               |
| Ответ в чат      | 5-10s   | Форматирование              |
| Твой коммит      | 2-5 мин | Копирование + git           |
| Проверка         | 10-15s  | Чтение GitHub               |
| ИТОГО:           | 3-7 мин | От запроса до синхронизации |

🎯 Оптимизация workflow
Когда я НЕ делаю полный аудит:
Работаем в одной сессии

Версия не изменилась

Контекст сохранён

Пример:

Ты: "Исправь X"
Я: [аудит] + решение

Ты: "Готово"
Я: [проверка] + подтверждение

Ты: "Теперь исправь Y"
Я: [БЕЗ аудита] + решение  ← быстрее!

Когда присылаю diff вместо полного файла:
Если изменение точечное (1-5 строк):

📝 ИЗМЕНЕНИЕ (diff)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 scripts/corrections/speaker_classifier.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Строка 156:
- return merged_segments
+ return merged_segments if merged_segments else []

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Хочешь полный файл или diff достаточно?

Ты выбираешь:

"Diff достаточно" - быстрее

"Дай полный файл" - безопаснее

❓ FAQ
Q: Что если я забуду обновить VERSION.md?
A: При следующем запросе я прочитаю GitHub и напомню:

⚠️ ВНИМАНИЕ: Обнаружено несоответствие версий
   speaker_classifier.py: v16.6 (в комментариях)
   VERSION.md: v16.5

Обнови VERSION.md перед продолжением работы!

Q: Можно ли коммитить несколько файлов одновременно?
A: Да! Я присылаю все изменённые файлы одним блоком:

✅ РЕШЕНИЕ: 4 файла для копирования
━━━ 1. speaker_classifier.py ━━━
━━━ 2. boundary_fixer.py ━━━
━━━ 3. docs/VERSION.md ━━━
━━━ 4. docs/MODULES.md ━━━

Q: Как часто нужно делать аудит?
A: Только при первом запросе в сессии или после долгого перерыва.

Q: Что если я закоммитил не все файлы?
A: Я увижу это при проверке и напомню:

⚠️ Не все файлы закоммичены:
   ✅ speaker_classifier.py
   ❌ docs/VERSION.md (не обновлён)
   ❌ docs/MODULES.md (не обновлён)