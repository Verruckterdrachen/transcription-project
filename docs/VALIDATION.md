# VALIDATION.md - Методология проверки качества транскрипции

## 🎯 ЦЕЛЬ
Чеклист для AI и человека: что проверить ПЕРЕД тем, как считать результат готовым.

---

## ✅ ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ (v16.20+)

### 1. TIMESTAMP CONTINUITY
**Что проверяем:** Нет ли разрывов или слишком длинных блоков?

**Команды:**
```python
# Найти блоки >30 сек без промежуточных timestamp
for seg in segments:
    duration = seg['end'] - seg['start']
    if duration > 30:
        print(f"⚠️ Длинный блок: {seg['time']} ({duration:.1f}s)")
Нормальные значения:

✅ Блоки 5-30 сек (идеально)

⚠️ Блоки 30-60 сек (проверить контекст)

❌ Блоки >60 сек (АНОМАЛИЯ - требует split)

Типичные баги:

Блоки 100-200 сек → нет промежуточных меток

Сдвиг timestamp после gap filling (+5-10 сек)

2. TEXT PATTERNS (Заикания, дубли, hallucinations)
Что проверяем: Типичные артефакты Whisper

Паттерны для поиска:

text
# 1. ДУБЛИ (одно и то же предложение 2 раза)
"(.{20,})\\.\\s*\\1"  # Ищем повтор фразы >20 символов

# 2. ЗАИКАНИЯ (слово повторяется 2-3 раза)
"\\b(\\w+)\\s+\\1\\b"  # "начинает начинает"
"\\b(\\w+)\\s+\\1\\s+\\1\\b"  # "я я я"

# 3. ENDING HALLUCINATIONS
"продолжение следует"
"спасибо за внимание"
"подписывайтесь на наш канал"
"до новых встреч"

# 4. ЛОГИЧНЫМ ЛОГИЧНЫМ (разный регистр)
"\\b(\\w+)\\s+\\1\\b" (case-insensitive)
Инструменты:

bash
# Поиск дублей в TXT
grep -E "(.{20,})\. \1" "Исаев (27.05).txt"

# Поиск заиканий
grep -oE "\b(\w+) \1\b" "Исаев (27.05).txt"

# Поиск ending hallucinations
grep -i "продолжение следует" "Исаев (27.05).txt"
3. SPEAKER CONSISTENCY
Что проверяем: Нет ли смежных реплик одного спикера?

Проверка:

python
# Найти смежные реплики одного спикера
for i in range(len(segments) - 1):
    if segments[i]['speaker'] == segments[i+1]['speaker']:
        print(f"⚠️ Смежные {segments[i]['speaker']}: {segments[i]['time']} и {segments[i+1]['time']}")
Нормальные случаи:

✅ Исаев → Журналист → Исаев (правильно)

⚠️ Исаев → Исаев (АНОМАЛИЯ - проверить, почему не merged)

Типичные баги:

merge_replicas() не склеил из-за большой паузы

split_mixed_speaker_segments() разбил неправильно

classification ошибочно сменил speaker

4. ABSOLUTE TIMESTAMP VERIFICATION
Что проверяем: Совпадают ли timestamp в TXT с реальным аудио?

Методология:

Открой аудио файл в аудиоредакторе (Audacity, Adobe Audition)

Выбери 3-5 случайных timestamp из TXT

Перейди на эти timestamp в аудио

Проверь: текст совпадает с тем, что говорится?

Пример проверки:

text
TXT: [00:10:03] Ситуация была прежней...
AUDIO: 00:10:03 → "Ситуация была прежней..." ✅

TXT: [00:21:36] учитывать была немецкая артиллерия...
AUDIO: 00:21:36 → "...берегу Невы" (сдвиг!) ❌
AUDIO: 00:21:39 → "учитывать была..." (правильный timestamp) ✅
Типичные сдвиги:

После gap filling: +3-10 сек

После overlap adjustment: +1-5 сек

Накопительная ошибка: +0.1-0.5 сек на каждый merge

5. GAP ANALYSIS
Что проверяем: Нет ли пропущенных кусков аудио?

Проверка:

python
# Найти гапы >3 сек
for i in range(len(segments) - 1):
    gap = segments[i+1]['start'] - segments[i]['end']
    if gap > 3.0:
        print(f"🚨 GAP: {segments[i]['time']} → {segments[i+1]['time']} ({gap:.1f}s)")
Нормальные значения:

✅ Гапы 0-1 сек (пауза в речи)

⚠️ Гапы 1-3 сек (проверить контекст)

❌ Гапы >3 сек (ПРОПУЩЕН ТЕКСТ - требует force_transcribe)

Действия:

Если gap >3 сек → проверить, был ли force_transcribe_diar_gaps()

Если gap остался → возможно silence (тишина в аудио)

6. STRUCTURAL PATTERNS
Что проверяем: Правильная ли структура диалога?

Нормальные паттерны:

text
Журналист → Исаев → Журналист → Исаев ...
Исаев (длинный блок 30-60 сек) → Журналист (короткий 5-10 сек) → Исаев ...
Аномалии:

text
Исаев → Исаев (должны были merged!)
Журналист (60 сек блок) ← слишком длинно для вопроса
Исаев (5 сек) → Журналист (5 сек) → Исаев (5 сек) ← слишком частая смена
7. TEXT QUALITY
Что проверяем: Читаемость и осмысленность текста

Признаки проблем:

Обрывы предложений: "И тогда они"

Бессмысленные фразы: "и и и и"

Неправильная пунктуация: "слово.слово.слово."

Пропущенные слова: "был операция"

📊 CHECKLIST ДЛЯ AI (после обработки)
ОБЯЗАТЕЛЬНО ПРОВЕРИТЬ ПЕРЕД "Готово!":

 Длинные блоки: нет блоков >30 сек без промежуточных timestamp

 Дубли: нет повторяющихся фраз (grep дубли)

 Заикания: все заикания имеют "..." (grep заикания)

 Ending hallucinations: нет "Продолжение следует" и подобных

 Смежные реплики: нет двух реплик одного спикера подряд

 Timestamp drift: случайная проверка 3-5 timestamp в аудио

 Гапы: все гапы >3 сек объяснены (force_transcribe или silence)

Если хотя бы 1 пункт НЕ выполнен → FIX перед коммитом!

🐛 ИЗВЕСТНЫЕ ПАТТЕРНЫ БАГОВ (v16.20)
Whisper Hallucinations
"Продолжение следует" (конец файла)

"Спасибо за внимание" (конец файла)

"Подписывайтесь на наш канал" (YouTube-контекст)

Дубли при заиканиях: "ничего не знали. ничего не знали."

Timestamp Issues
Сдвиг после gap filling (+3-10 сек)

Длинные блоки без промежуточных меток (>100 сек)

Накопительная ошибка в merge (0.1-0.5 сек на merge)

Speaker Attribution
False split на "вы " внутри слов (Невы, совы)

Continuation phrase не детектируется (порог 80% → 90%)

Mixed-speaker в анонсах вопросов ("А вот что вы скажете...")

Text Artifacts
Заикания без "...": "начинает начинает" вместо "начинает... начинает"

Логичным Логичным (разный регистр не детектируется)

Loop artifacts: "и и и и и и"

🔧 ИНСТРУМЕНТЫ ДЛЯ ПРОВЕРКИ
1. Python скрипты
python
# scripts/validation/check_results.py
# TODO: создать автоматический валидатор
2. Bash команды
bash
# Длинные блоки
python -c "import json; [print(f'{s[\"time\"]} ({s[\"end\"]-s[\"start\"]:.1f}s)') for f in ['file.json'] for s in json.load(open(f))['segments_merged'] if s['end']-s['start']>30]"

# Дубли
grep -E "(.{20,})\. \1" "Исаев (27.05).txt"

# Смежные реплики
python -c "import json; segs=json.load(open('file.json'))['segments_merged']; [print(f'{segs[i][\"time\"]} и {segs[i+1][\"time\"]}') for i in range(len(segs)-1) if segs[i]['speaker']==segs[i+1]['speaker']]"
3. Manual verification
Audacity / Adobe Audition (timestamp проверка)

Diff tool (сравнение версий)

Grep (поиск паттернов)

📚 СВЯЗАННЫЕ ДОКУМЕНТЫ
docs/KNOWN-ISSUES.md — детальное описание каждого типа бага

docs/DEBUGGING.md — методология отладки при обнаружении бага

docs/TESTING-STRATEGY.md — стратегия unit testing

Последнее обновление: 12.02.2026
Версия: v16.20
Автор: AI Assistant