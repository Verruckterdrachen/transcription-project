## v16.24.1 (2026-02-13)
**🔧 FIX #2: Adjacent same speaker - neutral фразы после split**

### ROOT CAUSE (5 Whys):

**Симптом:**
Ожидалось: Исаев → Журналист → Исаев (3 сегмента)
Получено: Исаев → Журналист (2 сегмента)

text

**5 Whys:**
1. Почему третье предложение не создаёт сегмент Исаев?  
   → Попадает в группу Журналиста

2. Почему попадает в группу Журналиста?  
   → Наследует `current_speaker="Журналист"`

3. Почему наследует Журналиста?  
   → Текст neutral, логика использует `current_speaker`

4. Почему использует `current_speaker` а не original?  
   → Нет переменной `original_speaker`

5. **ROOT CAUSE:** Отсутствие запоминания исходного спикера сегмента

### РЕШЕНИЕ:

**scripts/corrections/boundary_fixer.py:**
- Добавлена переменная `original_speaker = speaker` (строка 248)
- Neutral фразы теперь используют `original_speaker` вместо `current_speaker`
- Reason: `"neutral (return to original)"`

**Логика:**
Сегмент: speaker="Исаев"

"Первое" → neutral → original_speaker="Исаев" ✅

"Расскажите" → journalist → split to "Журналист" ✅

"Третье" → neutral → original_speaker="Исаев" ✅ (FIX!)

text

### Testing:
```bash
python -m pytest tests/test_boundary_fixer.py::test_raw_speaker_id_mapping -v
✅ Ожидаемый результат: PASSED (3 сегмента)
v16.24 (2026-02-13)
🔧 FIX #1: Дубль timestamp в начале сегмента

## v16.24 (2026-02-13)
**🔧 FIX #1: Дубль timestamp в начале сегмента**

### ROOT CAUSE (5 Whys):

**Симптом:** 
00:00:56 00:00:56 То есть это было...

text

**5 Whys:**
1. Почему два одинаковых timestamp?  
   → Inner timestamp вставлен в начало текста сегмента

2. Почему вставлен в начало?  
   → Условие `time_since_last >= 25` выполнилось для первого предложения

3. Почему выполнилось для первого предложения?  
   → Первое предложение длинное (~30 сек)

4. Почему так близко к началу?  
   → Нет проверки минимального расстояния от `start_sec`

5. **ROOT CAUSE:** Отсутствие проверки `sent_start - start_sec >= 2.0`

### РЕШЕНИЕ:

**scripts/export/txt_export.py:**
- Добавлена проверка `is_too_close_to_start = (sent_start - start_sec) < 2.0`
- Inner timestamp НЕ вставляется если < 2 секунд от начала сегмента
- Условие `should_insert` теперь включает `not is_too_close_to_start`

**tests/test_txt_export.py:**
- Новый тест: `test_no_duplicate_timestamp_at_segment_start()`
- Создаёт очень длинное первое предложение (~35 сек)
- Проверяет: inner timestamp >= 2 сек от начала

### Testing:
```bash
python -m pytest tests/test_txt_export.py::test_no_duplicate_timestamp_at_segment_start -v
✅ Ожидаемый результат: PASSED
v16.23.2 (2026-02-13)
🧪 FIX: Unit tests исправлены - GREEN