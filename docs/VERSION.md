# Version: v16.28
# Last updated: 2026-02-16

## 🔧 v16.28: FIX БАГ #3 - Потеря последнего предложения при timestamp injection
**Дата:** 2026-02-16  
**ROOT CAUSE:** `range(0, len(sentences)-1, 2)` пропускает последний элемент массива  
**РЕШЕНИЕ:** Изменить на `range(0, len(sentences), 2)` для обработки ВСЕХ элементов

### Проблема v16.27:

**Симптом:**
AFTER MERGE:
"...и ничего с ней сделать не могли осенью и в начале зимы 1941 года. То есть это был такой пункт, который делал для немцев оборону этого участка Невы достаточно простым точки зрения техники и тактики."
1572 символов, 227 слов ✅

AFTER TIMESTAMP INJECTION:
"...и ничего с ней сделать не могли осенью и в начале зимы 1941 года."
1462 символов, 208 слов ❌
→ ПОТЕРЯНО: "То есть это был такой пункт..." (110 символов, 19 слов)

text

### 5 Whys Root Cause Analysis:

1. **Почему пропала фраза "то есть это был такой пункт"?**  
   → `insert_intermediate_timestamps()` обрезала последнее предложение

2. **Почему обрезало последнее предложение?**  
   → `range(0, len(sentences)-1, 2)` пропускает последний элемент

3. **Почему пропускает последний элемент?**  
   → `len(sentences)-1` исключает последний индекс из обработки

4. **Почему использовали `-1`?**  
   → Ошибка в логике: хотели избежать выхода за границы, но создали баг

5. **ROOT CAUSE:** Неправильный range в list comprehension → потеря последнего предложения при парсинге текста!

### РЕШЕНИЕ v16.28:

**scripts/corrections/timestamp_fixer.py:**
```python
# ❌ БЫЛО (v16.27):
sentences = [''.join(sentences[i:i+2]).strip() for i in range(0, len(sentences)-1, 2)]

# ✅ СТАЛО (v16.28):
sentences = [''.join(sentences[i:i+2]).strip() for i in range(0, len(sentences), 2)]
Почему это работает:

Пример (7 элементов):

python
sentences = ['Текст 1', '.', ' ', 'Текст 2', '.', ' ', 'Текст 3']

# ❌ range(0, 7-1, 2) = range(0, 6, 2) = 
# i=0: sentences[0:2] = ['Текст 1', '.']
# i=2: sentences[2:4] = [' ', 'Текст 2']
# i=4: sentences[4:6] = ['.', ' ']
# НЕ обрабатывается: sentences = 'Текст 3' ← ПОТЕРЯНО!

# ✅ range(0, 7, 2) = 
# i=0: sentences[0:2] = ['Текст 1', '.']
# i=2: sentences[2:4] = [' ', 'Текст 2']
# i=4: sentences[4:6] = ['.', ' ']
# i=6: sentences[6:8] = ['Текст 3'] ← СОХРАНЕНО!
РЕЗУЛЬТАТ:

text
AFTER TIMESTAMP INJECTION:
"...и ничего с ней сделать не могли осенью и в начале зимы 1941 года. То есть это был такой пункт, который делал для немцев оборону этого участка Невы достаточно простым точки зрения техники и тактики."
1572 символов, 227 слов ✅ Текст ПОЛНЫЙ!
Изменения:

✅ range(0, len(sentences), 2) обрабатывает ВСЕ элементы

✅ Последнее предложение НЕ теряется

✅ Текст остаётся полным после timestamp injection

Урок v16.28:

List comprehension с range(0, len(arr)-1, step) опасен!

Всегда используй range(0, len(arr), step) если нужны ВСЕ элементы

Debug checkpoints помогли найти ТОЧНОЕ место потери данных

# Version 16.28.3

**Date**: 2026-02-16  
**Status**: 🔍 DEBUG - Детальное отслеживание txt_export

## 🎯 Изменения

### Детальный DEBUG в insert_inner_timestamps
- Автоматическая детекция целевого диапазона [00:02:30-00:04:40]
- Показ исходного текста (до обработки)
- Показ процесса разбиения на предложения
- Показ вставки timestamps
- Показ финального текста (после обработки)
- Проверка наличия целевой фразы ДО и ПОСЛЕ
- Диагностика: если фраза потеряна → показывает ГДЕ именно

### Цель
Найти происходит ли потеря фразы "то есть это был такой пункт" в txt_export

## 📋 Что смотреть

🎯 TXT EXPORT TARGET RANGE: [00:02:33-00:04:39]
✅ Целевая фраза НАЙДЕНА в исходном тексте!
...
❌ Целевая фраза ПОТЕРЯНА после обработки!
🔍 Фраза была в предложении #X: "..."
❌ Предложение ПОТЕРЯНО при сборке result!

---

# Version 16.28.2

**Date**: 2026-02-16  
**Status**: 🔍 DEBUG - Детальное отслеживание merge_replicas

## 🎯 Изменения

### Детальный DEBUG в merge_replicas
- Автоматическая детекция целевого диапазона (00:04:29 = 269s)
- Показ КАЖДОГО добавляемого сегмента с preview текста
- Показ текста ДО/ПОСЛЕ clean_loops
- Показ текста ДО/ПОСЛЕ clean_hallucinations_from_text
- Проверка наличия целевой фразы в финальном тексте

### Цель
Найти ГДЕ ИМЕННО теряется фраза "то есть это был такой пункт"

---

# Version 16.28

**Date**: 2026-02-16  
**Status**: 🔍 DEBUG - Поиск места потери реплик

## 🎯 Изменения

### DEBUG CHECKPOINT System
- Добавлена функция `debug_checkpoint()` для отслеживания сегментов
- Checkpoint после КАЖДОГО этапа pipeline (14 точек)
- Автоматический поиск целевых timestamp (00:04:29, 00:10:44)
- Показ preview текста (первые 50 символов)

### Цель
Найти НА КАКОМ ЭТАПЕ исчезают реплики:
- 00:04:29 "То есть это был такой пункт, который делал ля немцев..."
- 00:10:44 "То есть накопленный опыт он, конечно, тоже влиял на то..."

## 📋 Как использовать

1. Запусти `python scripts/transcribe.py`
2. Смотри вывод checkpoint'ов:
🔍 CHECKPOINT [AFTER MERGE]: 245 сегментов
✅ 00:04:29: НАЙДЕН в [00:04:01-00:04:39] "То есть это был..."
✅ 00:10:44: НАЙДЕН в [00:10:03-00:10:49] "То есть накопленный..."

🔍 CHECKPOINT [AFTER SPLIT]: 312 сегментов
❌ 00:04:29: НЕ НАЙДЕН! (возможно удалён на предыдущих этапах)

text
3. Найди ПЕРВЫЙ checkpoint где появляется ❌
4. Это и есть этап-виновник!

## 🔬 ROOT CAUSE

После запуска с checkpoint будет известно:
- Этап-виновник
- Причина удаления
- Решение

---

# Version: v16.27
# Last updated: 2026-02-13

## 🎓 v16.27: Новая стратегия тестирования - Real tests > Unit tests
**Дата:** 2026-02-13  
**ROOT CAUSE v16.22-v16.26:** Unit tests давали ложное чувство безопасности  
**РЕШЕНИЕ:** Отказ от unit tests для багов + обязательный Real test ПЕРЕД коммитом

### Проблема v16.22-v16.26:

**8 итераций неудачных попыток:**
- v16.22: FIX timestamp дубли → unit tests GREEN → real test → ❌ НОВЫЙ БАГ!
- v16.23: FIX hallucination loops → unit tests GREEN → real test → ❌ НОВЫЙ БАГ!
- v16.24: FIX timestamp is_too_close → unit tests GREEN → real test → ❌ НОВЫЙ БАГ!
- v16.24.1: FIX adjacent same speaker → unit tests GREEN → real test → ❌ НОВЫЙ БАГ!
- v16.25: ОТКАТ + простой FIX → real test → ❌ Дубли остались!
- v16.26: FIX синхронизация → real test → ✅ Работает!

**Вывод:** 8 итераций вместо 1-2, потому что доверяли pytest GREEN без real test!

**Файлы с "зелёными" unit tests, которые НЕ помогли:**
- `tests/test_txt_export.py` - 5/5 PASSED, но дубли timestamp остались
- `tests/test_replica_merger.py` - 3/3 PASSED, но inner timestamps конфликтовали
- `tests/test_hallucinations.py` - 6/6 PASSED, но не видел взаимодействие модулей

### 5 Whys Root Cause Analysis:

1. **Почему 8 итераций?** → Unit tests GREEN, но real test ломался
2. **Почему не помогли unit tests?** → Изолировали функции от реального контекста
3. **Почему не видели проблему?** → НЕ тестировали взаимодействие replica_merger + txt_export
4. **Почему не запускали real test ПЕРЕД коммитом?** → Доверяли pytest GREEN
5. **ROOT CAUSE:** Неправильная стратегия - фокус на unit tests вместо real tests!

### РЕШЕНИЕ v16.27:

**Новая философия:**
```
OLD: Баг → Unit test → pytest GREEN → Коммит → Real test → ❌
NEW: Баг → DEBUG → ROOT CAUSE → FIX → Real test → ✅ Коммит
```

**Изменения v16.27:**
- ✅ **Удалены unit tests для багов** (7 файлов из tests/)
  - `test_txt_export.py` - удалён (баги взаимодействия)
  - `test_replica_merger.py` - удалён (баги взаимодействия)
  - `test_hallucinations.py` - удалён (баги взаимодействия)
  - `test_timestamp_fixer.py` - удалён (баги взаимодействия)
  - `test_boundary_fixer.py` - удалён (баги взаимодействия)
  - `test_speaker_classifier.py` - удалён (баги взаимодействия)
  - `test_deduplicator.py` - удалён (баги взаимодействия)

- ✅ **Оставлены только regex utils tests** (`test_regex_patterns.py`)
  - Утилиты можно тестировать изолированно
  - Регулярные выражения не зависят от модулей

- ✅ **ОБЯЗАТЕЛЬНЫЙ Real test ПЕРЕД коммитом**
  - Запуск `python scripts/transcribe.py` на реальном аудио
  - Проверка VALIDATION.md (7 пунктов) = условие для коммита
  - Коммитим ТОЛЬКО если checklist GREEN

- ✅ **Новые документы:**
  - `docs/INTEGRATION-CHECKLIST.md` - чеклист для AI перед отправкой решения
  - `docs/GOLDEN-DATASET.md` - методология regression testing
  - `docs/TESTING-STRATEGY.md` - обновлена (Real > Integration > Unit)
  - `docs/DEBUGGING.md` - добавлена секция "Когда НЕ создавать unit test"

**Правило v16.27:**
- ❌ Unit tests для багов взаимодействия модулей
- ✅ Unit tests ТОЛЬКО для изолированных утилит
- ✅ Real test ПЕРЕД КАЖДЫМ коммитом
- ✅ 7 пунктов VALIDATION.md = условие для коммита

### Почему это правильно:

**Unit tests НЕ ВИДЯТ:**
- Взаимодействие `replica_merger` + `txt_export`
- Реальные данные (длинные merged segments с inner timestamps)
- Полный pipeline (10 этапов от аудио до TXT)
- Накопленные эффекты от предыдущих этапов

**Real tests ПОКАЗЫВАЮТ:**
- Реальные баги на реальных данных
- Взаимодействие всех модулей
- Результат ПОЛНОГО pipeline
- Проблемы которые unit tests не могут поймать

**Пример v16.22-v16.26:**
```python
# Unit test PASSED:
def test_insert_inner_timestamps_no_duplicates():
    # Искусственные данные, изолированная функция
    result = insert_inner_timestamps(text, 0, 100, True)
    assert result.count("00:00:") == 1  # ✅ GREEN

# Real test FAILED:
# replica_merger УЖЕ добавил timestamps
# txt_export НЕ знает об этом
# → ДУБЛЬ timestamp в реальном TXT!
```

### Урок v16.27:

**Unit tests ≠ гарантия работы!**  
**Real tests = единственная настоящая гарантия!**

---

# Version: v16.26
# Last updated: 2026-02-13

## 🔧 v16.26: FIX дублей timestamp - синхронизация replica_merger + txt_export
**Дата:** 2026-02-13  
**ROOT CAUSE:** Два модуля добавляют inner timestamps независимо!  
**РЕШЕНИЕ:** txt_export проверяет наличие timestamp перед вставкой

### Проблема v16.25:

**Симптом:**
```
00:00:56 00:00:56 То есть это было...
00:01:22 Невский пятачок... 00:01:29
00:03:05 И с электростанции... 00:03:07
```

### 5 Whys Root Cause Analysis:

1. **Почему дубли `00:00:56 00:00:56`?** → Timestamp добавляется ДВАЖДЫ
2. **Почему дважды?** → И `replica_merger` И `txt_export` добавляют inner timestamps
3. **Почему оба добавляют?** → `replica_merger` (v16.19 ЭТАП 6.1), `txt_export` (v16.3+)
4. **Почему раньше не было дубля?** → До v16.19 только `txt_export` добавлял timestamps
5. **ROOT CAUSE:** Два независимых модуля добавляют timestamp → нужна синхронизация!

### РЕШЕНИЕ v16.26:

**scripts/export/txt_export.py:**
```python
def insert_inner_timestamps(text, start_sec, end_sec, next_segment_exists):
    # ✅ v16.26: Проверяем наличие inner timestamps от replica_merger
    has_inner_timestamps = bool(re.search(r'\d{2}:\d{2}:\d{2}', text))

    if has_inner_timestamps:
        return text  # УЖЕ есть timestamps от replica_merger (ЭТАП 6.1)!

    # ... остальная логика v16.25 (без изменений) ...
```

**Почему это работает:**
- `replica_merger` (ЭТАП 6.1) добавляет timestamps в длинные merged segments (>30 сек)
- `txt_export` теперь ПРОВЕРЯЕТ наличие timestamp ПЕРЕД вставкой
- Если timestamp УЖЕ есть → не добавляем (защита от дублей!)
- Если timestamp НЕТ → добавляем (короткий merged segment)

**РЕЗУЛЬТАТ:**
```
00:00:56 То есть это было... ✅ Нет дубля!
00:01:22 Невский пятачок... ✅ Timestamps от replica_merger!
00:03:05 И с электростанции... ✅ Все корректно!
```

**Изменения:**
- ✅ `insert_inner_timestamps()` проверяет `re.search(r'\d{2}:\d{2}:\d{2}', text)`
- ✅ Если найдены timestamp → возвращает text БЕЗ изменений
- ✅ Если НЕ найдены → применяет логику v16.25

**Урок (продолжение):**
- UNIT TESTS НЕ НУЖНЫ!
- Unit tests тестируют ОДНУ функцию изолированно
- НЕ видят взаимодействие replica_merger + txt_export
- Создают ложное чувство безопасности

**ПРАВИЛЬНЫЙ ПОДХОД:**
- Запускать ПОЛНЫЙ pipeline на реальных данных
- Проверять JSON + TXT + LOG (Result Validation Checklist)
- Если найден баг → RCA (5 Whys) → FIX → проверка снова
- Golden dataset для регресс-тестирования

---

# Version: v16.25
# Last updated: 2026-02-13

## 🔄 v16.25: ОТКАТ txt_export.py + простой FIX дублей
**Дата:** 2026-02-13  
**ROOT CAUSE:** Все "фиксы" v16.22-v16.24 создали НОВЫЕ баги вместо решения!  
**РЕШЕНИЕ:** Откат к v16.3 + простое условие `i > 0`

### Проблемы v16.22-v16.24 (НАШИ "фиксы"):

**БАГ #1: Двойной timestamp `00:00:56 00:00:56`**
- v16.24: Добавили `is_too_close_to_start < 2.0`
- НЕ РАБОТАЕТ: первое предложение может быть >25 сек!
- Дубль всё равно вставляется

**БАГ #2: Разбиение на новые строки**
```
«Тайфун», начали наступление на столицу.
00:01:22 Невский пятачок приобрел...
```
- v16.23: Добавили `\n` перед inner timestamp
- ПРОБЛЕМА: Одна реплика разбивается на несколько строк!

**БАГ #3: Два timestamp подряд `00:01:22 ... 00:01:29`**
- Причина: пропорциональное распределение времени по символам
- Короткие предложения получают мало времени
- Условие `time_since_last >= 25` срабатывает дважды

### РЕШЕНИЕ v16.25 (ПРОСТОЕ!):

**scripts/export/txt_export.py:**
- Откатились к v16.3 (БЕЗ `\n`, БЕЗ `is_too_close_to_start`)
- Добавили ОДНО условие в `should_insert`:
  ```python
  should_insert = (
      i > 0 and  # 🆕 НЕ вставлять timestamp для первого предложения!
      time_since_last >= 25 and
      (time_to_end >= 30 or not next_segment_exists)
  )
  ```

**Почему это работает:**
- **БАГ #1 (дубли):** `i > 0` запрещает inner timestamp для первого предложения → дубля НЕТ!
- **БАГ #2 (новые строки):** Убрали `\n` → всё в одной строке!
- **БАГ #3 (два подряд):** Условие `time_since_last >= 25` работает от `last_timestamp_at`, который обновляется → второй timestamp НЕ вставится сразу

### 5 Whys Root Cause Analysis:

1. **Почему наши "фиксы" не работали?**
   → Unit tests прошли, но real test ломался

2. **Почему unit tests прошли?**
   → Искусственные данные, не реальный текст

3. **Почему не поймали баги?**
   → Тесты не проверяли реальные сценарии

4. **Почему добавили сложные проверки?**
   → Не поняли настоящий ROOT CAUSE

5. **Почему не откатились сразу?**
   → Доверились GREEN tests

**ROOT CAUSE:** Unit tests дают ложное чувство безопасности БЕЗ реальных тестов!

**Урок:**
ВСЕГДА запускать РЕАЛЬНЫЙ тест после unit tests!

---

[... остальные версии v16.24, v16.23, v16.22, v16.21 и т.д. остаются без изменений ...]
