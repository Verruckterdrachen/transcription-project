# 📌 ТЕКУЩАЯ ВЕРСИЯ: v16.10

**Последнее обновление:** 2026-02-11  
**Название:** Continuation Phrase Fix in Split

---

## 🆕 v16.10 - Continuation Phrase Fix in Split (2026-02-11)

### КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ

**Проблема:** `split_mixed_speaker_segments` неправильно определял спикера для continuation phrases ("То есть", "В частности") при разделении длинных сегментов. Continuation phrases получали спикера из контекста split, а не из оригинального монолога.

**Исправлено:**
- ✅ Добавлена функция `is_continuation_phrase()` для детекции 12 типов continuation phrases
- ✅ При split continuation phrase проверяется контекст предыдущего монолога (>60s)
- ✅ Если continuation phrase идёт после длинного монолога, используется спикер монолога
- ✅ Защита от переатрибуции при переговоре фраз спикером
- ✅ Счетчик исправленных continuation phrases в логе

**Файлы изменены:**
- `scripts/corrections/boundary_fixer.py`
- `scripts/transcribe.py`

**Детекция continuation phrases:**
- "То есть..."
- "В частности..."
- "Кроме того..."
- "Также..."
- "Иными словами..."
- "Более того..."
- "Помимо этого..."
- "При этом..."
- "Однако..."
- "Тем не менее..."
- "Впрочем..."
- "Другими словами..."

---

## v16.9 - Weighted Classification Continuation Fix (2026-02-11)

**Изменения:**
- ✅ Добавлена проверка continuation phrases в `apply_speaker_classification_v15`
- ✅ Защита continuation phrases если предыдущий монолог >60s
- ✅ Счетчик исправленных continuation phrases

**Ограничение:** Исправление работало только на ЭТАПЕ 7, но баг возникал на ЭТАПЕ 8 в split → **Исправлено в v16.10**

---

## v16.8.1 - Fix Log Copy Timing (2026-02-11)

**Исправлено:**
- ✅ Копирование debug.log в test-results ПОСЛЕ закрытия файла
- ✅ Исправлен порядок операций в `__main__`

---

## v16.8 - Debug Log + Long Monologue Fix (2026-02-11)

**Новое:**
- ✅ Автоматическое логирование всего pipeline в `transcription_debug.log`
- ✅ Копирование debug.log в test-results/latest/
- ✅ Monologue context protection для длинных монологов >60s
- ✅ GAP overlap protection

---

## 📋 CHANGELOG

Полная история версий: **[CHANGELOG.md](CHANGELOG.md)**
