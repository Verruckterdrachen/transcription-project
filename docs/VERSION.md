# VERSION v16.16

**Date:** 2026-02-11  
**Status:** ✅ PRODUCTION

---

## 🔥 v16.16: КРИТИЧЕСКИЙ FIX - Word Boundary в regex паттернах!

### 🐛 Найденная ошибка v16.15

**Проблема:** Regex паттерн `r'вы\s+'` ловил "вы " внутри других слов!

**Причина:** Отсутствие word boundary `\b` в начале паттернов

**Результат:** FALSE POSITIVE определение журналистской фразы

**Пример:**
Предложение: "То есть с небольшого пространства земли на восточном берегу Невы предполагалось..."
^^^^
Regex: r'вы\s+' → MATCH! (нашёл "вы " в слове "Невы")
Result: Journalist=True ❌ ОШИБКА!
Split: Исаев → Журналист ❌ НЕПРАВИЛЬНО!

text

**Также затронуты слова:**
- "со**вы **" → `Journalist=True` ❌
- "кро**вы **" → `Journalist=True` ❌
- "гра**вы **" → `Journalist=True` ❌

### ✅ Решение v16.16

**Добавлен `\b` (word boundary) во все regex паттерны:**

1. **is_journalist_phrase():**
   - `r'вы\s+'` → `r'\bвы\s+'` ✅
   - `r'расскажите'` → `r'\bрасскажите\b'` ✅
   - `r'объясните'` → `r'\bобъясните\b'` ✅
   - `r'как\s+вы'` → `r'\bкак\s+вы\b'` ✅
   - `r'почему\s+вы'` → `r'\bпочему\s+вы\b'` ✅
   - `r'что\s+вы'` → `r'\bчто\s+вы\b'` ✅
   - `r'давайте'` → `r'\bдавайте\b'` ✅
   - `r'смотрим'` → `r'\bсмотрим\b'` ✅

2. **is_expert_phrase():**
   - `r'я\s+(считаю|думаю|полагаю)'` → `r'\bя\s+(считаю|думаю|полагаю)\b'` ✅
   - `r'на\s+мой\s+взгляд'` → `r'\bна\s+мой\s+взгляд\b'` ✅
   - `r'по\s+моему\s+мнению'` → `r'\bпо\s+моему\s+мнению\b'` ✅

**Теперь:**
Предложение: "То есть с небольшого пространства земли на восточном берегу Невы предполагалось..."
Regex: r'\bвы\s+' → NO MATCH! (требуется граница слова перед "вы")
Result: Journalist=False ✅ ПРАВИЛЬНО!
Speaker: Исаев ✅ ПРАВИЛЬНО!

text

### 📝 Изменённые файлы

1. **`scripts/corrections/boundary_fixer.py`**
   - `is_journalist_phrase()`: добавлен `\b` во все 8 паттернов
   - `is_expert_phrase()`: добавлен `\b` во все 3 паттерна (кроме surname)
   - Комментарии: `# 🔥 v16.16: добавлен \b`

2. **`scripts/transcribe.py`**
   - Обновлена версия: `VERSION = "16.16"`
   - Обновлены описания изменений в docstring

3. **`docs/VERSION.md`**
   - Описание v16.16
   - Примеры ошибки и решения

---

## 🎯 Ожидаемый результат

**После v16.16:**
- Предложение "То есть с небольшого пространства земли на восточном берегу Невы..." → `Journalist=False` ✅
- Предложение остаётся за **Исаевым** ✅
- Сегмент 00:01:47 теперь правильно: `speaker="Исаев"` ✅
- TXT: строка 00:01:47 показывает **Исаев** ✅

**Проверка в test-results/latest/:**
00:01:47 Исаев: То есть с небольшого пространства земли...

text
✅ ПРАВИЛЬНО!

---

## ⏮️ История версий

### v16.15 (2026-02-11)
- Debug output для split - нашли виновника (FALSE POSITIVE в regex)

### v16.14 (2026-02-11)
- Speaker от самого длинного сегмента в replica_merger

### v16.13 (2026-02-11)  
- Синхронизация raw_speaker_id в speaker_classifier

[Полная история: docs/CHANGELOG.md]