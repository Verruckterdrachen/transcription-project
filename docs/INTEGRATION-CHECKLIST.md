# 📋 Integration Checklist - Что проверить перед отправкой решения

**Версия:** v16.27  
**Дата:** 2026-02-13  
**Философия:** Real tests > Unit tests  

---

## ✅ Для БАГОВ (ОБЯЗАТЕЛЬНО)

### Перед фиксом:
- [ ] Прочитал **DEBUGGING.md** — понял RCA методологию
- [ ] Добавил **DEBUG OUTPUT** в проблемный модуль
- [ ] Нашёл **ROOT CAUSE** через 5 Whys
- [ ] Понял ПОЧЕМУ баг возник (не гадаю!)

### После фикса:
- [ ] Исправил баг (НЕ несколько вещей одновременно!)
- [ ] Обновил **VERSION.md** (с описанием ROOT CAUSE)
- [ ] **ЯВНО НАПОМНИЛ:** "Запусти REAL TEST на аудио ПЕРЕД коммитом!"
- [ ] **ПОКАЗАЛ validation checklist** (7 пунктов из VALIDATION.md)

### Перед коммитом (для пользователя):
- [ ] Запустил **REAL TEST** на реальном аудио
- [ ] Проверил **7 пунктов из VALIDATION.md:**
   - Длинные блоки: нет >30 сек без timestamp
   - Дубли: `grep -E "(.{20,})\. \1" result.txt`
   - Заикания: все имеют "..."
   - Ending hallucinations: нет "Продолжение следует"
   - Смежные реплики: нет двух одного спикера подряд
   - Timestamp drift: 3-5 проверок в Audacity
   - Гапы: все >3 сек объяснены
- [ ] **Все 7 пунктов ✅ GREEN** → коммить
- [ ] **Хотя бы 1 пункт ❌ RED** → FIX → проверка снова

---

## 📝 Шаблон reminder для AI

### Для БАГА:

```
✅ РЕШЕНИЕ v16.X

━━━ 1. scripts/path/file.py ━━━
[полный код с фиксом]

━━━ 2. docs/VERSION.md ━━━
[обновлённая версия + ROOT CAUSE]

━━━ ПЕРЕД КОММИТОМ: Запусти REAL TEST! ━━━

1. Запусти transcribe.py на реальном аудио
2. Проверь VALIDATION.md checklist (7 пунктов):
   
   # Длинные блоки
   grep -oE "\[.*\]" result.txt | head -20
   
   # Дубли
   grep -E "(.{20,})\. \1" result.txt
   
   # Ending hallucinations
   grep -i "продолжение" result.txt
   
   # Смежные реплики (визуально в TXT)
   # Должно быть: Исаев → Журналист → Исаев
   # НЕ должно быть: Исаев → Исаев
   
   # Timestamp drift (в Audacity)
   # Проверь 3-5 случайных timestamp

✅ Если checklist GREEN → коммить
❌ Если RED → присылай ошибку, фиксим

━━━ Git команды (ТОЛЬКО ПОСЛЕ GREEN) ━━━

git add scripts/path/file.py docs/VERSION.md
git commit -m "🔧 vX.X: описание бага + ROOT CAUSE"
```

---

## ❌ Что ЗАПРЕЩЕНО

### Критические ошибки:
- ❌ Говорить "коммить" БЕЗ напоминания про REAL TEST
- ❌ Пропускать VALIDATION.md checklist
- ❌ Создавать unit tests для взаимодействия модулей
- ❌ Доверять GREEN unit tests БЕЗ real test
- ❌ Предполагать, что пользователь сам проверит

### Методологические ошибки:
- ❌ Исправлять БЕЗ понимания ROOT CAUSE
- ❌ Менять несколько вещей одновременно
- ❌ Гадать гипотезы без 5 Whys
- ❌ Пропускать DEBUG OUTPUT

---

## 🎯 Философия v16.27+

**OLD (v16.22-v16.24):**
```
Баг → Unit test → FIX → pytest GREEN → Коммит → Real test → ❌ НОВЫЙ БАГ!
```

**NEW (v16.26+):**
```
Баг → DEBUG → 5 Whys → ROOT CAUSE → FIX → Real test → Checklist → ✅ Коммит!
```

**Ключевое отличие:**
- **НЕ полагаемся на unit tests** для багов модульного взаимодействия
- **ОБЯЗАТЕЛЬНЫЙ real test** ПЕРЕД коммитом
- **Checklist GREEN** = условие для коммита

---

**Последнее обновление:** 2026-02-13  
**Версия чеклиста:** v16.27  
**Урок от v16.22-v16.26:** Unit tests ≠ гарантия работы!
