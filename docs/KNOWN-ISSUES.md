# KNOWN ISSUES - База знаний типичных багов

## 🎯 ЦЕЛЬ
Документация всех известных багов, их паттернов и причин. AI должен проверять результаты на эти паттерны!

---

## 📋 КАТЕГОРИИ БАГОВ

### 1. TIMESTAMP ISSUES

#### 1.1. Длинные блоки без промежуточных меток
**Паттерн:**
- Блоки >30 сек (иногда до 231 сек!)
- В TXT: один timestamp на весь блок

**Пример:**
[00:06:12] Исаев: [текст 231 секунду без меток]

text

**Причина:**
- `merge_replicas()` не проверяет длительность
- Нет intermediate timestamps

**Обнаружение:**

# Найти блоки >30 сек в JSON
python -c "import json; [print(f'{s[\"time\"]} ({s[\"end\"]-s[\"start\"]:.1f}s)') for s in json.load(open('file.json'))['segments_merged'] if s['end']-s['start']>30]"
Fix: insert_intermediate_timestamps() (v16.19+)

Версия исправления: v16.19
Статус: ✅ FIXED

1.2. Timestamp drift (сдвиг)
Паттерн:

TXT: [00:21:36], но в аудио это 00:21:39

Сдвиг +3-10 сек после gap filling

Пример:

text
LOG: 🚨 GAP 00:21:34–00:21:39 (5.0s)
LOG: ⚠️ GAP overlap с next existing, adjusted end: 1299.28s
LOG: 🔧 Adjusted: 1294.30-1301.06 → 1294.30-1299.28

Результат: 
TXT показывает [00:21:36], но текст начинается в аудио на 00:21:39
Причина:

force_transcribe_diar_gaps() меняет segment.end из-за overlap

Но не обновляет segment['start'] следующего сегмента

Все последующие timestamp сдвигаются

Обнаружение:

Открыть аудио в Audacity

Проверить 3-5 случайных timestamp из TXT

Если текст не совпадает → померить сдвиг

Fix: correct_timestamp_drift() (v16.19+)

Версия исправления: v16.19
Статус: ✅ FIXED

2. TEXT ARTIFACTS
2.1. Дубли (одно предложение 2 раза)
Паттерн:

text
ничего не знали. ничего не знали.
Причина:

Whisper галлюцинирует при заиканиях спикера

deduplicate_segments() не ловит дубли внутри одного текста

Обнаружение:

bash
grep -E "(.{20,})\. \1" result.txt
Fix: is_duplicate_phrase() в filter_hallucination_segments() (v16.19+)

Версия исправления: v16.19
Статус: ✅ FIXED

2.2. Заикания без "..."
Паттерн:

text
начинает начинает наступать
логичным Логичным решением
Причина:

detect_continuation_phrase() порог 80% пропускает similarity 85-95%

Разный регистр (Логичным vs логичным) не детектируется

Обнаружение:

bash
grep -oE "\b(\w+) \1\b" result.txt | sort | uniq
Fix:

Порог 80% → 90% (v16.19)

Case-insensitive comparison в similarity check

Версия исправления: v16.19
Статус: ✅ FIXED

2.3. Ending hallucinations
Паттерн:

text
Продолжение следует.
Спасибо за внимание.
Подписывайтесь на наш канал.
До новых встреч.
Ставьте лайки.
Причина:

Whisper "дофантазирует" конец файла

Обучался на YouTube видео с типичными фразами

Обнаружение:

bash
grep -i "продолжение следует\|спасибо за внимание\|подписывайтесь" result.txt
Fix: remove_ending_hallucinations() (v16.19+)

Версия исправления: v16.19
Статус: ✅ FIXED

2.4. Loop artifacts
Паттерн:

text
и и и и и и
то то то то
Причина:

Whisper зацикливается на паузах

VAD threshold слишком низкий

Обнаружение:

bash
grep -oE "\b(\w+)( \1){3,}\b" result.txt
Fix: clean_loops() в merge_replicas() (v16.1+)

Версия исправления: v16.1
Статус: ✅ FIXED

3. SPEAKER ATTRIBUTION
3.1. False split на "вы" внутри слов
Паттерн:

text
[00:01:47] Исаев: То есть с небольшого пространства земли на восточном берегу Не
[00:01:47] Журналист: вы
Причина:

is_journalist_phrase() regex r'вы\s+' ловил "вы " внутри "Невы"

Нет word boundary \b

Обнаружение:

Смежные реплики с одинаковым timestamp

Speaker меняется посреди предложения

Обрыв слова в конце реплики

Fix: Word boundary r'\bвы\s+' (v16.16+)

Версия исправления: v16.16
Статус: ✅ FIXED

3.2. Смежные реплики одного спикера
Паттерн:

text
[00:23:07] Исаев: ...
[00:23:53] Исаев: ...   # АНОМАЛИЯ! Должны были merged
Причина:

merge_replicas() не склеил из-за большой паузы (>5 сек для спикера)

Или split_mixed_speaker_segments() ошибочно разбил

Или classification изменил speaker после merge

Обнаружение:

python
for i in range(len(segments)-1):
    if segments[i]['speaker'] == segments[i+1]['speaker']:
        print(f"⚠️ АНОМАЛИЯ: {segments[i]['time']} и {segments[i+1]['time']}")
Fix: Проверить логику:

Merger rules (пауза threshold)

Split rules (false positives)

Classification weight

Версия исправления: Зависит от случая
Статус: ⚠️ REQUIRES INVESTIGATION

3.3. Continuation phrase не детектирован
Паттерн:

text
[00:15:22] Исаев: ...начинает наступать начинает
[00:15:26] Журналист: наступать...   # FALSE SPLIT!
Причина:

Порог similarity 80% пропускал заикания с 85-95% сходством

detect_continuation_phrase() возвращал False

Обнаружение:

Короткая реплика Журналиста (<10 слов)

Начинается с продолжения слова из предыдущей реплики

Similarity с концом предыдущей реплики >80%

Fix: Порог 80% → 90% (v16.19)

Версия исправления: v16.19
Статус: ✅ FIXED

4. MERGE/SPLIT BUGS
4.1. Зависание merge_replicas на последнем сегменте
Паттерн:

text
LOG:
  🔀 00:23:53 Исаев — начало merge
  [NO MORE OUTPUT]
Причина:

Последний сегмент: j >= len(segments), цикл не входит

clean_loops() или clean_hallucinations_from_text() зависают на длинном тексте

Нет debug output для диагностики

Обнаружение:

LOG обрывается после "начало merge"

Pipeline не доходит до следующих этапов

Fix:

Debug output перед тяжёлыми операциями (v16.20)

TODO: Timeout для очистки текста

Версия обнаружения: v16.20
Статус: 🔍 IN PROGRESS (debugging)

🔍 CHECKLIST ДЛЯ AI
Перед "Готово!" проверь на известные баги:

 Нет блоков >30 сек (bug 1.1)

 Timestamp совпадают с аудио (bug 1.2)

 Нет дублей (bug 2.1)

 Нет заиканий без "..." (bug 2.2)

 Нет ending hallucinations (bug 2.3)

 Нет loop artifacts (bug 2.4)

 Нет false splits на "вы" (bug 3.1)

 Нет смежных реплик одного спикера (bug 3.2)

Если нашёл → см. DEBUGGING.md для Root Cause Analysis!

📚 СВЯЗАННЫЕ ДОКУМЕНТЫ
docs/VALIDATION.md — методология проверки качества

docs/DEBUGGING.md — методология отладки

docs/CHANGELOG.md — история исправлений

Последнее обновление: 12.02.2026
Версия: v16.20
Автор: AI Assistant