# 🔍 DEBUGGING WORKFLOW

Методология поиска и исправления багов в проекте транскрибации.

---

## 🎯 ФИЛОСОФИЯ: ROOT CAUSE ANALYSIS FIRST

**❌ НЕПРАВИЛЬНО:**
```
Симптом → Гипотеза → Фикс → Тест → Новая гипотеза → ...
```

**✅ ПРАВИЛЬНО:**
```
Симптом → Почему? → Почему? → Почему? → ROOT CAUSE → Фикс → ✅
```

---

## 📋 DEBUGGING CHECKLIST

### Шаг 1: Воспроизведение проблемы

- [ ] **Точное описание симптома**
  - Что именно не работает? (конкретная строка, таймкод, спикер)
  - Какой ожидаемый результат?
  - Какой фактический результат?
  
- [ ] **Минимальный пример (minimal reproducible example)**
  - Файл: `test-results/latest/эксперт.txt`
  - Строка: `00:01:47 Журналист: То есть с небольшого...`
  - Ожидается: `00:01:47 Исаев: То есть с небольшого...`

- [ ] **Воспроизводимость**
  - Проблема повторяется каждый раз? ДА/НЕТ
  - На каких файлах проявляется?
  - Паттерн: когда появляется, когда нет?

---

### Шаг 2: Локализация проблемы

**Используй МЕТОД "5 ПОЧЕМУ" (5 Whys):**

```
Пример: 00:01:47 Журналист вместо Исаев

❓ ПОЧЕМУ спикер = Журналист?
→ Потому что произошёл SPLIT в boundary_fixer.py

❓ ПОЧЕМУ произошёл SPLIT?
→ Потому что is_journalist_phrase() = True

❓ ПОЧЕМУ is_journalist_phrase() = True для "То есть с небольшого... Невы"?
→ Потому что regex нашёл совпадение

❓ ПОЧЕМУ regex нашёл совпадение?
→ Потому что r'вы\s+' нашёл "вы " в слове "Невы"

❓ ПОЧЕМУ regex находит "вы " внутри слова?
→ Потому что НЕТ word boundary (\b) в начале паттерна

✅ ROOT CAUSE: отсутствие \b в regex паттернах
```

**🛠️ ИНСТРУМЕНТЫ:**

1. **DEBUG OUTPUT** - добавь детальный вывод в проблемный модуль
2. **LOG файлы** - `test-results/latest/эксперт_debug.log`
3. **JSON traces** - `test-results/latest/эксперт_*.json`
4. **Git blame** - когда код был изменён последний раз?
5. **Diff с предыдущей версией** - что изменилось?

---

### Шаг 3: Добавление DEBUG OUTPUT

**ПРАВИЛО:** Перед фиксом ВСЕГДА добавляй debug output!

```python
# ❌ ПЛОХО: Silent mode
def is_journalist_phrase(text):
    for marker in journalist_markers:
        if re.search(marker, text):
            return True
    return False

# ✅ ХОРОШО: Debug mode
def is_journalist_phrase(text, debug=False):
    """
    v16.16: Проверяет, является ли фраза журналистской
    
    Args:
        text: Текст для проверки
        debug: Вывод детальной информации
    """
    text_lower = text.lower()
    
    if debug:
        print(f"  🔍 is_journalist_phrase('{text[:50]}...')")
    
    for marker in journalist_markers:
        if re.search(marker, text_lower):
            if debug:
                print(f"    ✅ MATCH: {marker}")
            return True
    
    if debug:
        print(f"    ❌ NO MATCH")
    return False
```

**📊 DEBUG УРОВНИ:**

```python
# Уровень 1: Minimal (только результат)
print(f"✅ is_journalist_phrase() = {result}")

# Уровень 2: Medium (результат + причина)
print(f"✅ is_journalist_phrase() = {result} (reason: {marker})")

# Уровень 3: Detailed (весь процесс)
print(f"  🔍 Checking: '{text[:50]}...'")
for marker in markers:
    match = re.search(marker, text)
    print(f"    Marker: {marker} → {'✅' if match else '❌'}")
print(f"  → RESULT: {result}")
```

---

### Шаг 4: Создание Unit Test

**ПРАВИЛО:** Найдённый баг → СРАЗУ unit test!

```python
# tests/test_regex_patterns.py

def test_journalist_phrase_positive_cases():
    """Позитивные кейсы: должны определяться как журналистские"""
    assert is_journalist_phrase("вы можете объяснить") == True
    assert is_journalist_phrase("расскажите нам") == True
    assert is_journalist_phrase("давайте обсудим") == True

def test_journalist_phrase_false_positives():
    """Негативные кейсы: НЕ должны определяться как журналистские"""
    # BUG v16.15: "Невы" определялась как "вы"
    assert is_journalist_phrase("на восточном берегу Невы") == False
    assert is_journalist_phrase("совы охотятся ночью") == False
    assert is_journalist_phrase("у кровы есть крыша") == False
    assert is_journalist_phrase("правы были все") == False

def test_journalist_phrase_edge_cases():
    """Edge cases: пограничные ситуации"""
    # "вы" в конце предложения
    assert is_journalist_phrase("это были вы?") == True
    # "вы" в середине другого слова
    assert is_journalist_phrase("называется") == False
    # Multiple matches
    assert is_journalist_phrase("вы можете расскажите") == True
```

**🎯 КОГДА СОЗДАВАТЬ TESTS:**
- ✅ После каждого найденного бага
- ✅ Перед добавлением новой функции
- ✅ Для всех edge cases
- ✅ Для всех regex паттернов

---

### Шаг 5: Исправление (только после понимания ROOT CAUSE!)

**ПРОЦЕСС:**

1. **Написать фикс**
```python
# v16.16: Добавлен word boundary
journalist_markers = [
    r'\bвы\s+(можете|могли|должны)?',  # 🔥 добавлен \b
    r'\bрасскажите\b',
    # ...
]
```

2. **Проверить unit test**
```bash
pytest tests/test_regex_patterns.py::test_journalist_phrase_false_positives
# ✅ PASSED
```

3. **Проверить integration test**
```bash
python scripts/transcribe.py
# Проверить: 00:01:47 Исаев ✅
```

4. **Обновить VERSION**
```python
VERSION = "16.16"
VERSION_NAME = "Critical Fix: Word Boundary в regex"
```

5. **Коммит с описанием ROOT CAUSE**
```bash
git commit -m "🔥 v16.16: Critical fix - word boundary в regex (false positive 'Невы')

ROOT CAUSE: Regex r'вы\s+' находил 'вы ' внутри слов (Невы, совы, кровы)
FIX: Добавлен word boundary \b во все regex паттерны
RESULT: False positive устранён, предложение остаётся за Исаевым"
```

---

## 🚫 ТИПИЧНЫЕ ОШИБКИ

### ❌ Ошибка 1: Фикс без понимания ROOT CAUSE

```
Симптом: "00:01:47 Журналист вместо Исаев"
Гипотеза: "Проблема в continuation phrase logic"
→ Меняю continuation logic
→ Тест: проблема осталась
→ Новая гипотеза: "Проблема в raw_speaker_id"
... 8 итераций ...
```

**✅ ПРАВИЛЬНО:**
```
Симптом: "00:01:47 Журналист вместо Исаев"
→ Добавляю DEBUG output в split_mixed_speaker_segments()
→ Вижу: is_journalist_phrase=True для "Невы"
→ ROOT CAUSE: regex без word boundary
→ Фикс: добавляю \b
→ ✅ Проблема решена за 1 итерацию
```

---

### ❌ Ошибка 2: Изменение нескольких вещей одновременно

```
Commit: "Fix continuation phrase + raw_speaker_id sync + add debug"
→ Что именно помогло? Непонятно!
```

**✅ ПРАВИЛЬНО:**
```
Commit 1: "Add debug output to split function"
Commit 2: "Fix regex word boundary (ROOT CAUSE)"
→ Чёткое разделение: debug → фикс
```

---

### ❌ Ошибка 3: Фикс без unit test

```
→ Исправили баг
→ Закоммитили
→ Через 2 недели: баг вернулся (другой разработчик изменил regex)
```

**✅ ПРАВИЛЬНО:**
```
→ Исправили баг
→ Написали unit test
→ Закоммитили (код + тест)
→ Regression защита: баг не может вернуться
```

---

## 📊 DEBUGGING METRICS

**ЦЕЛЬ:** Сократить количество итераций от бага до фикса

### Метрики качества debugging:

| Метрика | Идеал | Текущий | Цель |
|---------|-------|---------|------|
| Итераций до фикса | 1-2 | 8 | ≤3 |
| Время на диагностику | 10-30 мин | 5.5 часов | ≤1 час |
| Unit tests coverage | 100% | 0% | ≥80% |
| False positives найдено тестами | 100% | 0% | ≥90% |

---

## 🛠️ DEBUGGING TOOLS

### 1. Debug Mode в функциях

```python
def function_name(..., debug=False):
    if debug:
        print(f"🔍 function_name: входные данные...")
```

### 2. Детальный LOG

```python
# transcribe.py
if mode == "точный":
    log_file = f"{latest_dir}/{speaker_surname}_debug.log"
    # Сохраняет ВЕСЬ терминальный вывод
```

### 3. JSON traces

```json
{
  "segments_raw": [...],  // До обработки
  "segments_merged": [...], // После merge
  "corrections_log": {...}  // Что изменилось
}
```

### 4. Git diff для сравнения

```bash
# Сравнить с предыдущей версией
git diff v16.15 v16.16 -- scripts/corrections/boundary_fixer.py
```

---

## 🎓 ПРИМЕРЫ ROOT CAUSE ANALYSIS

### Пример 1: False Positive в Regex

**Симптом:** `00:01:47 Журналист` вместо `Исаев`

**5 Whys:**
1. Почему спикер = Журналист? → SPLIT произошёл
2. Почему SPLIT? → is_journalist_phrase=True
3. Почему =True? → regex r'вы\s+' нашёл совпадение
4. Почему нашёл? → "Невы" содержит "вы "
5. Почему ловит внутри слова? → Нет word boundary

**ROOT CAUSE:** `r'вы\s+'` без `\b`  
**FIX:** `r'\bвы\s+'`

---

### Пример 2: Несинхронизация полей

**Симптом:** JSON показывает `speaker="Исаев"`, TXT показывает `SPEAKER_00`

**5 Whys:**
1. Почему TXT неправильный? → Использует raw_speaker_id
2. Почему raw_speaker_id не обновлён? → Функция меняет только speaker
3. Почему меняет только speaker? → Забыли обновить raw_speaker_id
4. Почему забыли? → Нет проверки синхронизации
5. Почему нет проверки? → Нет unit test для этого

**ROOT CAUSE:** Отсутствие синхронизации `speaker` ↔ `raw_speaker_id`  
**FIX:** Обновлять оба поля + создать reverse mapping

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### Recommended Reading:
- [The 5 Whys Technique](https://en.wikipedia.org/wiki/Five_whys)
- [Root Cause Analysis](https://en.wikipedia.org/wiki/Root_cause_analysis)
- [Test-Driven Development (TDD)](https://en.wikipedia.org/wiki/Test-driven_development)

### В проекте:
- `docs/TESTING-STRATEGY.md` - стратегия тестирования
- `docs/MODULES.md` - справочник функций
- `tests/test_regex_patterns.py` - примеры unit tests

---

**Последнее обновление:** 11.02.2026  
**Версия:** v16.17
