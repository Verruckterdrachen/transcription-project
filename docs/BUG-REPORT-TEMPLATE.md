# 🐛 BUG-REPORT-TEMPLATE.md — шаблон отчёта о баге

Цель: сократить время на поиск и фикс багов за счёт **полного отчёта в одном сообщении**.

---

## 1. Симптом

**Что именно не так?**

- Что ожидалось?
- Что получилось?
- Где (timestamp, спикер, вопрос)?

Пример:

```text
Фраза "то есть это был такой пункт" пропадает из TXT.

Timestamp: 00:04:29
Ожидалось:
"...и ничего с ней сделать не могли осенью и в начале зимы 1941 года.
То есть это был такой пункт, который делал..."

Получили:
"...и ничего с ней сделать не могли осенью и в начале зимы 1941 года."

Потеря: ~110 символов, ~19 слов.
2. Checkpoint‑логи
Вставь минимум два checkpoint’а:

ДО проблемного этапа;

ПОСЛЕ проблемного этапа.

Пример:

text
🔍 CHECKPOINT [AFTER MERGE]: 36 сегментов
   ✅ 00:04:29: НАЙДЕН в [00:02:33-00:04:39] (длит=126.0s, слов=227)
      📝 Конец: "...то есть это был такой пункт, который делал для немцев
      оборону этого участка Невы достаточно простым точки зрения техники и тактики."

🔍 CHECKPOINT [AFTER TIMESTAMP INJECTION]: 36 сегментов
   ✅ 00:04:29: НАЙДЕН в [00:02:33-00:04:39] (длит=126.0s, слов=208)
      📝 Конец: "...и ничего с ней сделать не могли осенью и в начале зимы 1941 года."

→ ПОТЕРЯ: ~110 символов, ~19 слов на этапе TIMESTAMP INJECTION.
3. Подозреваемые файлы / функции (если есть)
Если ты уже нашёл/подозреваешь место — укажи:

text
Файл: scripts/corrections/timestamp_fixer.py
Функция: insert_intermediate_timestamps
Строки: около 40–90 (парсинг предложений)
Можно приложить кусок кода:

python
sentences = re.split(r'([.!?]+)\s+', text)
sentences = [
    ''.join(sentences[i:i+2]).strip()
    for i in range(0, len(sentences)-1, 2)  # ← подозрительно: -1
]
4. Статус по VALIDATION.md
Проставь статус по каждому пункту:

text
✅ 1. Длинные блоки (>30s) — OK
❌ 2. Потеря текста — FAIL (пропало ~110 символов в одном сегменте)
✅ 3. Дубли timestamp — OK
✅ 4. Заикания — OK
✅ 5. Ending hallucinations — OK
✅ 6. Смежные реплики — OK
✅ 7. Timestamp drift — OK
5. test-results
Если возможно, приложи:

Название спикера/папки.

Содержимое test-results/latest/:

JSON;

TXT;

_debug.log (если включен TeeOutput).

Минимум — ссылка или название файлов:

text
test-results/latest/
  ├── NW_Uckpa0001_01.json
  ├── Исаев (27.05).txt
  └── Исаев (27.05)_debug.log
6. Что это даёт
Без шаблона:

много уточняющих вопросов;

60+ минут на локализацию и фикс.

С шаблоном:

этап‑виновник виден сразу по checkpoint’ам;

файл/функция известны заранее;

контекст VALIDATION.md понятен;

10–15 минут на анализ и фикс.

7. Мини‑чеклист перед отправкой баг‑репорта
 Есть точный пример (timestamp + ожидание/факт).

 Приведены checkpoint’ы ДО и ПОСЛЕ проблемного этапа.

 Указаны подозреваемые файлы/функции (если есть).

 Заполнен статус по 7 пунктам VALIDATION.md.

 Есть ссылки/названия файлов в test-results/latest/.